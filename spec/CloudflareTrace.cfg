CONSTANTS
    (* Set constants specific to Cloudflare *)
    Node <- TraceNode
    Nil <- TraceNil
    ConfigVersion = {v1, v2, v3}
    InitConfig = v1
    MaxRollouts = 5
    (* Set default action for TVOperators module *)
    Default <- CFDefault
    (* Set variables of TraceSpec module *)
    Vars <- vars
    (* Set operator for TraceSpec module *)
    BaseInit <- Init
    UpdateVariables <- CFUpdateVariables
    TraceNext <- CFTraceNext

SPECIFICATION
    TraceSpec

PROPERTIES
    BaseSpec

VIEW
    TraceView

CONSTRAINT 
    Termination

POSTCONDITION
    TraceAccepted

CHECK_DEADLOCK
    FALSE
